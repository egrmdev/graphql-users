query {
    users(first:1) {
        edges {
            node {
                id
                firstName
                lastName
                userName
                email
            }
            cursor
        }
        pageInfo {
            hasPreviousPage
            hasNextPage
            startCursor
            endCursor
        }
    }
}